#include <bits/stdc++.h>
using namespace std;

int main(int argc, char const *argv[])
{
    /* code */
    int a;
    cin >> a;
    // if a is positive
    if (a > 0)
    {
        cout << "A is positive" << endl;
        ;
    }
    // if a is negative
    else
    {
        cout << "A is not positive" << endl;
    }

    return 0;
}
