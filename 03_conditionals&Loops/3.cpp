#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b;
    cout << "Enter the values of a and b: ";
    cin >> a >> b;
    if (a > b)
    {
        cout << "A is greater" << endl;
    }
    if (b > a)
    {
        cout << "B is greater" << endl;
    }

    return 0;
}